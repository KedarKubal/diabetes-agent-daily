# 2026-09-22 — Passive multimodal glucose band (EDA + circadian + sex)

Fork-style feature on a simple GVU loop (inspired by openai/openai-agents-python patterns, no LLM required).

```bash
python3 gvu_eda_circadian.py
```

Verifier success: must cite EDA tonic, circadian hour, and sex; no food log; estimate applies the multimodal shift and stays in a Clarke A/B-plausible range.
